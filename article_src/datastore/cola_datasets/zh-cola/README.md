Taken from here https://github.com/huhailinguist/CoLAC.

However, since they do not provide a test set with label, we have merge the train and dev, and we have generated new
train-dev-test splits using a 60-10-30 ratio, and we do not use their test sets. See `zh_splits.py` for more details.